import React, { FunctionComponent } from "react";
import styled from "styled-components/native";
import { UsageSectionProps } from "./types";

const UseageSection: FunctionComponent = () => {
    return ();
};

export default UseageSection;
